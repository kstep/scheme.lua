return {
    base = require("scheme.defs.base"),
    math = require("scheme.defs.math"),
    lua  = require("scheme.defs.lua"),
    io   = require("scheme.defs.io"),
    iter = require("scheme.defs.iter"),
    exn  = require("scheme.defs.exn"),
}
